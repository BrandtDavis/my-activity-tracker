import React from 'react';
import { LoginPage } from './pages/LoginPage';

function App() {
  return (
    <div>
      <LoginPage />
      {/* {(typeof data.name !== "string") ? (
        <p>Loading...</p>
      ): (<h1>{data.name}</h1>
      )} */}

    </div>
  );
}

export default App;
